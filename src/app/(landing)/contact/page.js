import ContactPage from '@/components/landing/pages/ContactPage'

export const metadata = {
  title: 'Contact Us — GoFastDelivery',
  description: 'Get in touch with GoFastDelivery. Call, email, or send a message — our team responds within 2 hours.',
}

export default function ContactUsPage() {
  return <ContactPage />
}
